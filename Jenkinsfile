pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                sh 'git status'
            }
        }

        stage('Build') {
            steps {
                sh 'ls'
            }
        }
    }
}
